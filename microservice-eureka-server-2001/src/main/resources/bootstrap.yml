spring: 
  application:
    name: microservice-eureka-server-config-2001
  cloud:
    config:
      name: eureka_config_2001
      uri: http://configserver.volcano.com:4001  # 配置configserver地址
      profile: dev  # 级别
      label: master  # 分支 git中 默认master